apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx
  name: nginx
spec:
  ports:
    - name: port-1
      port: 80
      protocol: TCP
      targetPort: 8080
    - name: port-2
      port: 443
      protocol: TCP
      targetPort: 8443
  selector:
    app: nginx
